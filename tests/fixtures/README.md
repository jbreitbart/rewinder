# Media Fixtures

Use the generator to create/reset a realistic media tree for local testing:

```bash
tests/fixtures/generate_media_fixtures.sh --reset
```

Optional custom location:

```bash
tests/fixtures/generate_media_fixtures.sh --target /tmp/rewinder-media --reset
```
